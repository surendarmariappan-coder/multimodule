// Configuration is centralized in the root build.gradle for all modules.
description = 'DepartmentService'

base {
    archivesName = 'department-service'
}

dependencies {
    implementation project(':employee-service')
}

jar {
    enabled = true
}